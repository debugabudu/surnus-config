taskExecutor:
  corePoolSize: 5
  maxPoolSize: 10
  queueCapacity: 10

spring:
  cloud:
      stream:
        bindings:
          vusers-result-out:
            destination: vusers-result
  datasource:
    url: jdbc:mysql://localhost:3306/sturnus?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: root
    password: WmyRWws68JncZwSW4DMshisOZo1DnNil
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://${REGISTER_IP:localhost}:${REGISTER_PORT:9040}/eureka/
    healthcheck:
      enabled: true