taskExecutor:
  corePoolSize: 5
  maxPoolSize: 10
  queueCapacity: 10

eureka:
  client:
    serviceUrl:
      defaultZone: http://${REGISTER_IP:localhost}:${REGISTER_PORT:9040}/eureka/
    healthcheck:
      enabled: true